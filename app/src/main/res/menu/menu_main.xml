<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

  <item
    android:id="@+id/action_settings"
    android:orderInCategory="100"
    android:title="@string/action_settings"
    app:showAsAction="never"/>

  <item
    android:id="@+id/action_request_permission"
    android:orderInCategory="100"
    android:title="@string/action_request_permission"
    app:showAsAction="never"/>

  <item
    android:id="@+id/action_open_accessory"
    android:orderInCategory="100"
    android:title="@string/action_open_accessory"
    app:showAsAction="never"/>

  <item
    android:id="@+id/action_get_device_list"
    android:orderInCategory="100"
    android:title="@string/action_get_device_list"
    app:showAsAction="never"/>

</menu>
